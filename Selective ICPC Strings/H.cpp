#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
 
    string a, b;
    while(getline(cin, a) && getline(cin, b)){
        
    }

    return 0;
}